<nav class="navbar  navbar-expand-lg navbar navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/" class="d-flex align-items-center me-3 mb-lg-0 text-white text-decoration-none">
          <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" fill="Gold" class="bi bi-film" viewBox="0 0 16 16">
            <path d="M0 1a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1H1a1 1 0 0 1-1-1V1zm4 0v6h8V1H4zm8 8H4v6h8V9zM1 1v2h2V1H1zm2 3H1v2h2V4zM1 7v2h2V7H1zm2 3H1v2h2v-2zm-2 3v2h2v-2H1zM15 1h-2v2h2V1zm-2 3v2h2V4h-2zm2 3h-2v2h2V7zm-2 3v2h2v-2h-2zm2 3h-2v2h2v-2z"/>
          </svg>        
        </a>
        <div class="navbar-brand">
          <%= link_to "ToDo List", root_path, class: "navbar-brand btn btn-warning text-dark fw-bold" %>
        </div>
        <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto">
              <li class='nav-item'>
                <%= link_to 'Tasks', tasks_path, class:'btn btn-outline-warning me-2' %>
              </li>
              <li class='nav-item'>
                <%= link_to 'Projects', projects_path, class:'btn btn-outline-warning me-2' %>
              </li>

              <li class="nav-item">
                <% I18n.available_locales.each do |locale| %>
                  <% if I18n.locale != locale %>
                    <%= link_to t(locale), root_path(locale: locale), class: 'btn btn-outline-warning me-2' %>
                  <% end %>
                <% end %>
              </li>
              <% if user_signed_in? %>
                <%= link_to current_user.name.present? ? current_user.name : current_user.email , destroy_user_session_path, class: 'btn btn-outline-warning' %>
              <% else %>
                <li class="nav-item">
                <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-warning' %>
                </li>
              <% end %>
            </div>
        </div>
    </div>
</nav>
